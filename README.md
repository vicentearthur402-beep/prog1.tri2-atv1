# prog1.tri2-atv1

- no windows
 - crie uma pasta (temporaria)
 - na barra de endereço digite: cmd
 - então execute os comandos abaixo


 ```bash
  git clone https://<<git.token>>@github.com/vicentearthur402-beep/prog1.tri2-atv1
  projeto

  ```bash
  code --user-data-dir=vscode-data projeto
  ```

  # ...

  ```bash
  git push
  ```

  # ...

  ```bash
  git add --all
  git commit - m "<<descrição do que foi feito em aula>>>"
  gut push
  ```
